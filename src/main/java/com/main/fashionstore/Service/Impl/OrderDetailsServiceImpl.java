package com.main.fashionstore.Service.Impl;

import org.springframework.stereotype.Service;
import com.main.fashionstore.Service.OrderDetailsService;

@Service
public class OrderDetailsServiceImpl implements OrderDetailsService {

    // @Autowired
    // private OrderDetailsDao orderDetailsDao;

    // @Override
    // public void addOrderDetail(OrderDetails orderDetails) {
    //     // Thêm chi tiết đơn hàng vào cơ sở dữ liệu
    //     orderDetailsDao.save(orderDetails);
    // }

    // @Override
    // public List<OrderDetails> getOrderDetailsByOrderId(Integer orderId) {
    //     // Lấy danh sách chi tiết đơn hàng dựa trên ID đơn hàng
    //     return orderDetailsDao.findByOrderId(orderId);
    // }

    // Các phương thức khác tương tác với chi tiết đơn hàng
}
